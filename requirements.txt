fastapi==0.111.0
uvicorn[standard]==0.30.1
jinja2==3.1.4
python-dotenv==1.0.1
google-generativeai==0.7.2
pydantic==2.7.4
itsdangerous==2.2.0
psycopg2-binary==2.9.9

# NOTE: this file intentionally does NOT include torch/transformers.
# It's kept lean so it deploys cleanly on serverless platforms like Vercel
# (which cap function bundles at 500MB -- torch + transformers alone blow
# past that). This means the local LaMini-Flan-T5 fallback is disabled
# unless you also install requirements-local-fallback.txt.
#
# For local development with the fallback model enabled, run:
#   pip install -r requirements.txt -r requirements-local-fallback.txt

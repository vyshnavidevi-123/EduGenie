# Optional: only needed for the local LaMini-Flan-T5 fallback model.
# Install alongside requirements.txt for local development if you want the
# app to work without a Gemini API key:
#   pip install -r requirements.txt -r requirements-local-fallback.txt
#
# Do NOT include this on Vercel or other serverless deployments -- these
# packages are large (~3GB combined) and will exceed serverless function
# bundle size limits.
transformers==4.42.3
torch==2.3.1
sentencepiece==0.2.0
